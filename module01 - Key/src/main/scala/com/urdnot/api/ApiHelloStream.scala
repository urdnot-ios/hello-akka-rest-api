package com.urdnot.api

object ApiHelloStream {
//  def processParameter(message: String): String = {
//    Source.single(ByteString.fromString(message))
//
//    val suffixSource: Source[ByteString, NotUsed] = Source.single(ByteString.fromString("\n\nADDS THIS AFTER INCOMING ENTITY"))
//    // concat the suffix stream to the incoming entity stream
//
//    val replySource: Source[ByteString, NotUsed] = ByteString.fromString(message) + suffixSource
//    // prepare and return the entity:
//
//    val replyEntity = HttpEntities.create(ContentTypes.`text/plain(UTF-8)`, replySource)
//
//    return complete(StatusCodes.OK, replyEntity))
//  }
}
